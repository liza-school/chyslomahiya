# Hand-off: заняття 3 «Відра без поділок» (для Codex)

Стан на 2026-09-26. Усе закомічено й запушено в `main` (`liza-school/chyslomahiya`), GitHub Pages зібрано:
<https://liza-school.github.io/chyslomahiya/> → «Заняття 3». Перевірка: `cd course && node tools/check.js` — «Усе гаразд».

## Головне від Валерія

* **Пріоритет — компʼютер і великий планшет**, телефон другорядний (але `check.js` і далі стежить, щоб на 320 px нічого не різалось).
* Коміт/пуш/Pages у `liza-school` — без питань. Префікс комітів `[CHYS-0] …`, опис українською.
* Жодних plan/retro-файлів; документація — `docs/INDEX.md`, `docs/COURSE.md`, цей файл. `docs/features/` — чужі чернетки, не комітити й не видаляти.
* Скани підручника — `lesson3/page-11.png`, `page-12.png` (у git не йдуть, `/lesson*/` у `.gitignore`).
* Умови задач переписуються точно; антураж — Гаррі Поттер (українські імена за перекладом А-БА-БА-ГА-ЛА-МА-ГИ).

## Що є в занятті 3 (`course/js/lessons/l03.js`)

Джерело: підручник стор. 11 (приклади: 10/7 → 8 з озера; 14 л бензину, 9 і 5 → 3) і стор. 12 (задачі 1–8).
**Домашнє: № 2, 6, 7** (позначені `homework: true`).

Порядок блоків: історія (Мірта, багатозільне зілля) → домашнє → заклинання «Аква Циркулюс» (точне правило кола) →
«простими словами» → пісочниця 3/7 (зібрати числа 1–7) → приклад 1 (таблиця підручника) → квіз → **більярд** (теорія) →
**граф станів 3 і 4 → 2** (покроковий, дві доріжки 4 і 6 кроків) → пастка (Рон туди-сюди; Герміона доливає неповного давача) →
приклад 2 → симулятор бензину (ліміт 8) → квіз «що далі за правилом» → multi → НСД → **задачі 1–8** →
**рядок кола** (тільки після задач, навмисно) + квіз + input → понад підручник **9–14** → тренажер Аґуаменті → фінал, Minecraft, життя, підсумок.

Задачі понад підручник: 9 — 6/9 → 4 неможливо (НСД, інваріант); 10 — 10/7/3 навпіл (9 кроків); 11 — 7/11 → 6 за ≤ 10 кроків;
12 — по 2 л у відрах 4 і 9 неможливо («куля завжди на бортику») + (2; 9) на відрах; 13 — задача Пуассона 12/8/5 → 6 і 6
(7 кроків, стіл зі зрізаним кутом); 14 — довести, що 5/3 → 4 не швидше за 6 кроків (карта ходів).

Підказки до задач 1–8 **без готового рядка кола** — вчать «думати з кінця» (4 = 5 − 1 …). Так попросив Валерій:
спершу дитина пробує сама, правило вибору боку відкривається після задач.

## Рушій переливань (`course/js/jugs.js`, `course/css/jugs.css`)

* `JUGS.create(cfg)` — симулятор: кнопки набрати/вилити/перелити; після переливання з новим числом відро під «кришкою»
  і питання «скільки тепер» (лити далі не можна); таблиця кроків як у підручнику; повтор стану червоніє; ліміт; «Крок назад»;
  після розвʼязку — найкоротший шлях (BFS). Поруч живий **більярд** (стан — точка, крок — відрізок, номери кроків,
  під кришкою лише напрямок). На ширині ≥ 700 px більярд праворуч від відер.
* `cfg`: `vessels`, `start`, `source` (`lake|tap|river`, без нього — замкнена система з великою посудиною першою),
  мета `target`(+`targetIn`) | `exact` | `collect`, `limit`, `liquid` (`petrol|milk|butterbeer|pumpkin`), `where`, `intro`, `forbidden`.
* `JUGS.table(cfg, ["F0","P01","E1",…])`, `JUGS.billiard(cfg, ходи)` — для розвʼязань; літри рахує рушій, хибний хід → помилка в консолі → `check.js` падає.
* `JUGS.stateMap(cfg, глибина)` — карта ходів рядами (№ 14). `JUGS.walk(cfg)` — покроковий граф станів (блок `jugwalk`).
* `JUGS.trainer` — тренажер 1–20 л, НСД, «рівно найкоротший» ліміт. `JUGS.shortest`, `circleRow`, `gcd`.
* Типи блоків у `engine.js`: `jugs`, `jugtrainer`, `jugwalk`; у задачі — поле `jugs`. Описано в `COURSE.md`.

## Перевірені перебором факти (не переписувати без нового перебору)

* Дві посудини з озером, усі пари до 20 л: досяжне ⇔ ділиться на НСД і ≤ більшого; точне коло (давача доливають лише порожнім,
  приймач виливають лише повним) доходить без повторів; коротше з двох кіл = найкоротший шлях; недосяжне — коло замикається.
* Рядок кола: ближче до початку — мале у велике, до кінця — велике в мале, рівно посередині — мале (ніколи не довше).
* Граф рядами для двох відер — ширина 1, 2, 3, 2, 2, … = дві доріжки = два кола.
* Три посудини без озера: коло завжди доходить, але ≈5 % випадків не найкоротше (8/3/1 → 4: 2 кроки проти 7) — сказано в тексті.

## Відкрите / можна робити далі

1. **Живий граф у кожному симуляторі** (перемикач «Більярд / Граф», поточний стан, крок назад по рядах видно як марний).
   Валерій бачив цю пропозицію, явно не підтвердив — спершу спитати. Графи в занятті малі (≤ 36 станів, ≤ 4 в ряду, до 17 рядів).
2. Оглянути все заняття саме на **компʼютері/великому планшеті**: чи не задрібні більярд і граф (зараз граф до 580 px, більярд ≤ 230 px стола).
3. Неоднозначність «відлити X л» у задачах 3, 4, 8: зараховується й коли X лишилося у великій посудині. Чекаємо думки вчительки.
4. Більярд підписано просто «більярд» — автора не вказано навмисно (Перельман не підтверджено; класика — Твіді, 1939, теж не перевірено).
5. Далі — заняття 4, коли Валерій принесе матеріал (`lesson4/`, `l04.js`, правила в `COURSE.md`).
